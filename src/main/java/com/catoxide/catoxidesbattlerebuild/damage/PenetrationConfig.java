package com.catoxide.catoxidesbattlerebuild.damage;

public class PenetrationConfig {
    public static void loadFromJson(String configPath) {
        // TODO: 从JSON文件加载配置
        // Map<DamageType, PenetrationLevel> mappings = loadFromJsonFile(configPath);
        // PenetrationResolver.loadFromMap(mappings);
    }
}
